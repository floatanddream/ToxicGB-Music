# 环境变量模板。
#
# 这里的值已经是可直接用的默认值，所以**本地开发不需要复制这个文件**。
# 只有要覆盖某个值时，才复制成 `.env.local`（已在 .gitignore 里）再改 ——
# Vite 会优先读 `.env.local`，它不会进版本库。
#
# 变量名必须以 VITE_ 开头才会被前端代码读到（`import.meta.env.VITE_XXX`）。

# ── 浏览器侧：应用请求的 API 前缀 ──────────────────────────────
# 前端所有请求都打到这个前缀下，再由 Vite 开发代理转发到后端。
# 保持 /api 即可，除非你要把它挪到别的路径。
VITE_GLOB_API_URL=/api

# ── 服务端侧：Vite 开发代理的目标地址 ──────────────────────────
# 也就是 NeteaseCloudMusicApi 跑在哪。
#
#   - 后端跑在本机（默认 3000 端口）：不用改
#   - 后端跑在另一台机器上：改成那台机器的地址，例如 http://192.168.1.45:3000
#
# 注意这是**开发服务器（Node 进程）**去访问的地址，不是浏览器访问的地址，
# 所以你用手机连你的开发机调试时也不用改它。
VITE_PROXY_TARGET=http://localhost:3000
