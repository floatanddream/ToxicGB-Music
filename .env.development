# 开发环境默认值。要覆盖请建 .env.local（已 gitignore），见 .env.example。

# 浏览器侧：应用请求的 API 前缀，由 Vite 开发代理转发到后端
VITE_GLOB_API_URL=/api

# 服务端侧：Vite 开发代理的目标，即 NeteaseCloudMusicApi 跑在哪
VITE_PROXY_TARGET=http://localhost:3000
