# API代理地址
# 本地开发代理，可以解决跨域及多地址代理
# 如果接口地址匹配到，则会转发到http://localhost:30000，防止本地出现跨域问题
# 可以有多个，注意多个不能换行，否则代理将会失效
VITE_PROXY=[["/api","http://192.168.1.45:3000"]]
VITE_BASE=/
# 接口地址
# VITE_GLOB_API_URL=/api
VITE_GLOB_API_URL=/api

VITE_GLOB_NO_PREFIX_API_URL=http://192.168.1.45:3000